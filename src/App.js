import './App.css';
import Header from './components/Header';

function App() {
  return (
    <div className="App">
      {/* header */}
      <Header />
      {/* items => snapping */}
      {/* items => snapping */}
      {/* items => snapping */}
      {/* items => snapping */}
    </div>
  );
}

export default App;
